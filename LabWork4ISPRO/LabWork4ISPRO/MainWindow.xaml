<Window x:Class="LabWork4ISPRO.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LabWork4ISPRO"
        mc:Ignorable="d"
        Title="Пользователи и Роли" Height="450" Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Пользователи и их Роли" FontSize="24" Margin="10" FontWeight="Bold"/>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="10">
            <TextBox x:Name="SearchTextBox" Width="300" Margin="0,0,5,0" ToolTip="Поиск по логину или email..."/>
            <Button x:Name="SearchButton" Content="Поиск" Width="75" Click="SearchButton_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="2" 
                  x:Name="UserDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID Пользователя" Binding="{Binding UserId}" Width="*"/>
                <DataGridTextColumn Header="Логин" Binding="{Binding Login}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="*"/>
                <DataGridTextColumn Header="Роль" Binding="{Binding Role.Name}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
